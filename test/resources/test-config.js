// not napespaced
export let config = {
  'test/resources/test-config': {
    foo: {
      bar: 'bazzing',
      buz: 'buzzing'
    }
  }
};

export let configure = function configure(use, userConfig) {};
